# Todos 任务清单 VUE开发思路

- 响应式数据驱动界面开发
- v-if /v-for/v-model
  composition API
    @keydown.enter="addTodo"
    :class
    computed
    
    VUE 开发方式 简单高效，好上手

- 传统做法
  先找到目标元素，然后对其做修改（dom）

- vue做法
  不再思考页面的元素怎么操作，而是要思考数据是怎么变化的

- v-for 指令用于循环输出数组中的元素
  v-for 指令的语法：
- computed 计算属性
  - 性能更好，依赖的数据变化时才会重新计算


# 原型继承

JavaScript 的继承机制与其他面向对象语言（如 Java、C++）有所不同，它基于**原型（prototype）**而不是类（在 ES6 之前）。ES6 引入了 class 语法糖，但底层仍然是基于原型的继承。

- call,apply
  - 函数的方法，可以指定函数运行时的this指向
  - 被指定的函数也立即执行了
  - 参数不同，
    第一个参数是this的指向对象 构造函数继承中this 指向子类
    call的余下参数是一个个传
    apply的余下参数是是一个数组
    call()是逗号分隔，apply()是数组传递

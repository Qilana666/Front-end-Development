# react 组件通信

- 跨层级通信 useContext

  - 父子组件通信思想尝试解决了跨层级通信的问题
  - 一路传，性价比不高，麻烦 长安的荔枝
  - 规矩不变 父组件（外层组件）复杂持有和改变数据
  - 大于父子层级，传递的路径太长

- 数据在查找的上下文里，在最外层，可以提供给任何里面的任何的层级组件，随便用
- 要消费数据状态的组件拥有找数据的能力(传递是被动接收)

通信：父子 子父 兄弟 跨层级 。。。

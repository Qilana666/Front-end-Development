/\*\*

- @param {number[]} nums
- @return {void} Do not return anything, modify nums in-place instead.
  \*/
  var moveZeroes = function (nums) {
  let stackSize = 0;
  for (const x of nums) {
  if (x !== 0) {
  nums[stackSize++] = x; // 把 x 入模拟栈，原地覆盖
  }
  }
  nums.fill(0, stackSize);
  };
